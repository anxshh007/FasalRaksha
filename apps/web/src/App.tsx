/**
 * Application root. The interface is built from P9 onward on the design system ported from
 * Phase 1 (docs/PHASE1-STUDY.md); until then this renders the product's name and nothing that
 * pretends to compute (Constitution §1, §18).
 */
export function App() {
  return (
    <main>
      <h1 lang="mr">फसल रक्षा</h1>
      <p lang="en">Fasal Raksha — offline-first price intelligence and a verified-buyer marketplace for the field gate.</p>
    </main>
  );
}
