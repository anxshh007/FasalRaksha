# Nightly pipeline — Python 3.11+ (PROMPT §3.2). Exact pins: the validation table is only
# meaningful if two runs on two machines use the same numerical stack (PROMPT §14.2).
numpy==2.3.5
pandas==2.3.3
scikit-learn==1.7.2
lightgbm==4.6.0
onnx==1.19.1
onnxruntime==1.23.2
pytest==8.4.2
